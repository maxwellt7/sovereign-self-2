[build]
builder = "NIXPACKS"
buildCommand = "cd packages/shared && pnpm build && cd ../../apps/api && pnpm db:generate"

[deploy]
startCommand = "cd apps/api && pnpm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

