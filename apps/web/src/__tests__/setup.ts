import { beforeAll, afterEach, afterAll } from 'vitest';
import { cleanup } from '@testing-library/react';

beforeAll(() => {
  // Setup
});

afterEach(() => {
  cleanup();
});

afterAll(() => {
  // Cleanup
});

