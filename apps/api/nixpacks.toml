[phases.setup]
nixPkgs = ["nodejs-20_x", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = [
  "pnpm --filter @sovereign-self/shared build",
  "pnpm --filter api db:generate",
  "pnpm --filter api build"
]

[start]
cmd = "cd apps/api && pnpm start"

